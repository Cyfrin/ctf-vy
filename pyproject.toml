[project]
name = "ctf-vy"
version = "0.1.0"
description = "A minimal vyper implementation for creating vyper CTFs"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "mamushi>=0.0.3",
    "moccasin>=0.3.2",
]
